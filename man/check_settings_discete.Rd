% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ptmc_discrete.R
\name{check_settings_discete}
\alias{check_settings_discete}
\title{Check and Set Default Values for PTMC Settings}
\usage{
check_settings_discete(settings, model)
}
\arguments{
\item{settings}{A list of settings for the PTMC simulation. The function checks and fills missing settings with
default values. The following settings can be provided:
\itemize{
\item \code{numberChainRuns}: The number of chains to run in parallel.
\item \code{numberCores}: The number of CPU cores to use for parallel processing (default is equal to \code{numberChainRuns}).
\item \code{numberTempChains}: The number of temperature chains for the Parallel Tempering (default is 10).
\item \code{iterations}: The number of iterations for each chain (default is 20,000).
\item \code{burninPosterior}: The burn-in period for posterior samples (default is 10,000).
\item \code{thin}: The thinning interval for samples (default is 100).
\item \code{consoleUpdates}: The frequency of console updates (default is 100).
\item \code{numberFittedPar}: The number of parameters to fit in the model. If not specified, it is set to the length
of \code{model$namesOfParameters}.
\item \code{onAdaptiveCov}: Whether to use adaptive covariance updates (default is TRUE).
\item \code{updatesAdaptiveCov}: The frequency of updates for the adaptive covariance (default is 100).
\item \code{burninAdaptiveCov}: The burn-in period for adaptive covariance updates (default is 2,000).
\item \code{onAdaptiveTemp}: Whether to use adaptive temperature updates (default is TRUE).
\item \code{updatesAdaptiveTemp}: The frequency of updates for adaptive temperature (default is 10).
\item \code{onDebug}: Whether to enable debugging output (default is FALSE).
\item \code{lowerParBounds}: The lower bounds for the parameters. If not specified, it is set to \code{model$lowerParSupport_fitted}.
\item \code{upperParBounds}: The upper bounds for the parameters. If not specified, it is set to \code{model$upperParSupport_fitted}.
\item \code{covarInitVal}: The initial covariance value (default is 1e-10).
\item \code{covarInitValAdapt}: The initial adaptive covariance value (default is 1e-10).
\item \code{covarMaxVal}: The maximum allowed covariance value (default is 1).
\item \code{runParallel}: Whether to run the simulation in parallel (default is TRUE).
\item \code{lengthDiscreteVec}: The length of the discrete vector, which should be specified as \code{model$discrete_length}.
\item \code{updateDiscreteFreq}: The frequency at which to update the discrete vector (default is 0).
}}

\item{model}{A list representing the model used in the PTMC simulation. The function checks if specific model-related
attributes are available to fill in missing settings (e.g., \code{lowerParSupport_fitted}, \code{upperParSupport_fitted},
and \code{discrete_length}).}
}
\value{
A list of validated and updated settings. If any setting was missing or invalid, the corresponding default
value is filled in, and a message is printed to the console.
}
\description{
This function checks the provided settings list for missing values and assigns default values where necessary.
It ensures that all required settings for the Parallel Tempering Monte Carlo (PTMC) simulation are specified
and provides appropriate defaults if any are missing. The function also validates specific parameters by checking
the model for any necessary attributes.
}
\examples{
# Example usage of check_settings_discete function
model <- list(
  namesOfParameters = c("param1", "param2"),
  lowerParSupport_fitted = c(-5, -5),
  upperParSupport_fitted = c(5, 5),
  discrete_length = 10
)
settings <- list(
  numberChainRuns = 4,
  numberCores = 4,
  numberTempChains = 10,
  iterations = 20000,
  burninPosterior = 10000,
  thin = 100
)
validated_settings <- check_settings_discete(settings, model)

}
